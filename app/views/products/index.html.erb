<div>
  <h1>All Products</h1>

  <% @products.each do |p| %>
    <div>
      <img src="<%= p.image_url %>" width="100" height="100" />
      <p>
        <%= link_to p.name, product_path(p) %>
        $<%= p.amount %>
      </p>
    </div>
  <% end %>
</div>
